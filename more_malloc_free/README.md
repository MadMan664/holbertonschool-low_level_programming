# 0x0b. C - More malloc, free

Continuation of dynamic memory allocation in C: safe malloc wrapping with
`exit`, partial-string concatenation, a hand-rolled `calloc`, and building
an integer range array.
