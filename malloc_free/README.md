# 0x0a. C - malloc, free

Project covering dynamic memory allocation in C using `malloc` and `free`:
creating and freeing heap-allocated arrays, duplicating and concatenating
strings, and allocating/freeing 2D grids.
